version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npx ng build --configuration production
      artifacts:
        baseDirectory: dist/gym-bro-analytics/browser
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
